const sum= (a,b)=> a+b; 
const greeting= (name)=> `Hello ${name}!`
const isEven= (num)=> (num % 2 === 0)
const animals= ['cat', 'dog', 'horse']
module.exports= {
    sum,
    greeting,
    isEven,
    animals
}